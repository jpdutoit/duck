extern struct frequency;

extern function hz(float) -> frequency;

module AA {
  output: float;
}
aa: AA;
hz(aa);
//TODO: hz(AA) should work too

f1: frequency = 10 hz;
f2: frequency = hz(10);
f1 = f2;
